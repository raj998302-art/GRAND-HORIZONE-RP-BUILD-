# Grand Horizon RP — .env.example
# Copy to .env and fill real values. NEVER commit the real .env.
# Server-side secrets (DB passwords, etc.) belong on the server, NEVER in the APK.

# --- Public SA-MP endpoint (safe to ship in the client) ---
SERVER_HOST=142.132.203.47
SERVER_PORT=14448

# --- Public launcher update endpoint ---
MANIFEST_URL=https://cdn.grandhorizonrp.example/manifest.json
LAUNCHER_VERSION=1.0.0

# --- Server-side secrets (DO NOT ship in client) ---
DB_HOST=
DB_PORT=3306
DB_NAME=
DB_USER=
DB_PASSWORD=
RCON_PASSWORD=

# --- Build/signing (reference only — real values in GitHub Secrets) ---
KEYSTORE_BASE64=
KEYSTORE_PASSWORD=
KEY_ALIAS=
KEY_PASSWORD=
